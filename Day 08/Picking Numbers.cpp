#include <bits/stdc++.h>

using namespace std;

//int A[105];

int main(){
    int n;
    cin >> n;
    
    int a[n];    
    for(int i = 0;i < n;i++){
      // int x;
        cin >> i;
        
      //  A[x]++;
    }
    
    int pos=0,max=0,count=1;
    sort(a,a+n);
    for(int i=1; i<=n; i++)
        ans = max(aA[i]+A[i-1]);
    
    cout << ans << endl;
    
    return 0;
}
